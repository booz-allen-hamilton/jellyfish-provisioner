require 'spec_helper'

describe Jellyfish::Provisioner do
  it 'has a version number' do
    expect(Jellyfish::Provisioner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
